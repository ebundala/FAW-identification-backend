

enum QuestionType {
  BOOLEAN
  TEXT
}

enum Role {
  USER
  ADMIN
  MODERATOR
  ORGANIZATION
}

enum State {
  PENDING
  REVIEW
  REJECTED
  APPROVED
  COMPLETED
  ARCHIVED
}

enum OrderByInput{
  asc
  desc
}